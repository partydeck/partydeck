name: CI

on:
  pull_request:
  push:
    branches: ['**']
    tags-ignore: ['v*']
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  holo:
    runs-on: ubuntu-latest
    container: ghcr.io/steamdeckhomebrew/holo-toolchain-rust@sha256:818de45e147f35798c66498380b31bd1fe8bf75afdf0a22b027053706836faf1
    steps:
      - name: Install git
        run: pacman -Sy --noconfirm --needed git

      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Build
        id: build
        uses: ./.github/actions/build-holo
        with:
          version: UNOFFICIAL

      - name: Upload tarball
        uses: actions/upload-artifact@v4
        with:
          name: partydeck-UNOFFICIAL-SteamOS-${{ github.sha }}
          path: ${{ steps.build.outputs.tarball }}

  appimage:
    runs-on: ubuntu-latest
    container: archlinux:base-devel
    steps:
      - name: Install git
        run: pacman -Sy --noconfirm --needed git

      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Build
        id: build
        uses: ./.github/actions/build-appimage
        with:
          version: UNOFFICIAL

      - name: Upload AppImage
        uses: actions/upload-artifact@v4
        with:
          name: partydeck-UNOFFICIAL-anylinux-${{ github.sha }}
          path: ${{ steps.build.outputs.appimage }}
